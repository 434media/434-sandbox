import { HeroSection } from "@/components/HeroSection"

// Home = the design-system reference: the real 434 hero video, under the shared
// navbar, above the shared footer (both in app/layout.tsx). Build from here.
export default function Home() {
  return <HeroSection />
}
